import { AnnouncementItem } from '../types';

// Empty by default — announcements are published by administrators via the admin dashboard.
// Backend integration will populate this array from the database.
export const mockAnnouncements: AnnouncementItem[] = [];
